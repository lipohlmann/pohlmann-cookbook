\documentclass{admin/recipe_book}

\usepackage[type={CC},modifier={by},version={4.0}]{doclicense}

\usepackage[style=mla,sorting=none]{biblatex}
\addbibresource{pohlmann-cookbook.bib}

\usepackage[acronym,toc]{glossaries}
\makeglossaries
\include{admin/acronyms}
\usepackage{xcolor}
\renewcommand*{\glstextformat}[1]{\textcolor{deepcharcol}{#1}} % disabling the blue color of glossaries

\DeclareRobustCommand{\degF}{\ensuremath{^{\circ}\mathrm{F}}}

% Set universal colors
\definecolor{cream}{HTML}{eae5d9}
\definecolor{rust}{HTML}{631a19}
\definecolor{darkgreen}{HTML}{404932}
\definecolor{warmgray}{HTML}{8d9891}
\definecolor{deepcharcol}{HTML}{2e2727}


% ── Global page and text color ────────────────────────────────
\pagecolor{cream}
\color{deepcharcol}

% ── xcookybooky recipe colors ─────────────────────────────────
\setRecipeColors{%
  recipename    = darkgreen,  % recipe title
  inghead       = darkgreen,  % "Ingredients" heading
  prephead      = darkgreen,  % "Preparation" heading
  suggestionhead = darkgreen, % "Suggestion" heading
  hinthead      = rust,       % "Hint" heading
  hintline      = rust,       % hint box border lines
  numeration    = rust,       % step numbers in preparation
  separationgraph = warmgray, % the divider graphic
}

\usepackage{hyperref}    % must be the last package
\hypersetup{%
    pdfauthor            = {Liam Pohlmann},
    pdftitle             = {Pohlmann Cookbook},
    pdfsubject           = {Recipes},
    % pdfkeywords          = {example, recipes, cookbook, xcookybooky},
    pdfstartview         = {FitV},
    pdfview              = {FitH},
    pdfpagemode          = {UseNone}, % Options; UseNone, UseOutlines
    bookmarksopen        = {true},
    % pdfpagetransition    = {Glitter},
    colorlinks           = {true},
    linkcolor            = {black},
    urlcolor             = {blue},
    citecolor            = {black},
    filecolor            = {black},
}

\usepackage[capitalize]{cleveref}

\setcounter{secnumdepth}{0}

\hbadness=10000	% Ignore underfull boxes



\begin{document}
\include{admin/titlepage}

\clearpage
\doclicenseThis
\customtableofcontents

\input{admin/outline.tex}

\clearpage
\printbibliography[heading=bibintoc,title={References}]

\end{document}