% ============================================================
%  Cookbook Title Page
%  Drop this file into your main document and include it with:
%    \include{titlepage}   or   \input{titlepage}
%
%  Required packages (add to your preamble if not already there):
%    \usepackage{geometry}
%    \usepackage{xcolor}
%    \usepackage{fontenc}      % [T1]
%    \usepackage{microtype}
%    \usepackage{graphicx}     % only needed if you add an image
%    \usepackage{tikz}         % for the decorative rules
% ============================================================

% Written with the help of ClaudiAI Sonnet 4.6 generative AI.

\begin{titlepage}
  \centering
  \vspace*{2.8cm}

  % ── Top ornamental rule ────────────────────────────────────
  \begin{tikzpicture}
    \draw[color_2, line width=0.6pt] (0,0) -- (10,0);
    \draw[color_2, line width=1.6pt] (0,-0.18) -- (10,-0.18);
    \draw[color_2, line width=0.6pt] (0,-0.36) -- (10,-0.36);
  \end{tikzpicture}

  \vspace{0.55cm}

  % ── Subtitle / series label ────────────────────────────────
  {
    \large\scshape\color{color_4}
    A Collection of Liam's Favorite Recipes}

  \vspace{0.8cm}

  % ── Main title ─────────────────────────────────────────────
  {
    \Huge\bfseries\color{color_3}
    Pohlmann Family\\[0.25em]
    \huge\mdseries\itshape\color{color_2}
    Cookbook}

  \vspace{0.7cm}

  % ── Bottom ornamental rule ─────────────────────────────────
  \begin{tikzpicture}
    \draw[color_2, line width=0.6pt] (0,0) -- (10,0);
    \draw[color_2, line width=1.6pt] (0,-0.18) -- (10,-0.18);
    \draw[color_2, line width=0.6pt] (0,-0.36) -- (10,-0.36);
  \end{tikzpicture}

  \vspace{2.2cm}

  % ── Optional: swap the \rule for an actual image ───────────
  % \includegraphics[width=6cm]{your-image-file}
  %
  % Placeholder art block (remove if you add an image above)
  \textcolor{color_2!30}{%
    \begin{tikzpicture}
      \draw[color_2!25, line width=0.8pt, rounded corners=2pt]
      (0,0) rectangle (6,5);
      \node[color_2!40] at (3,2.5)
      {\itshape\large [cover image]};
    \end{tikzpicture}}

  \vspace{2.4cm}

  % ── Author ─────────────────────────────────────────────────
  {
    \large\color{color_4} Compiled by}\\[0.4em]
  {
  \Large\bfseries\color{color_3} Liam Pohlmann}

  \vfill

  % ── Footer line ────────────────────────────────────────────
  {
    \small\color{color_4} \the\year}

  \vspace{1.2cm}

\end{titlepage}
